export enum ParameterFormat {
    custom = 'custom',
    decimal = 'decimal',
    percent = 'percent',
    time = 'time',
    date = 'date',
    datetime = 'datetime',
    integer = 'integer',
    currency = 'currency',
    exponential = 'exponential',
    scientific = 'scientific',
    mantissa = 'mantissa',
    exponent = 'exponent',
    boolean = 'boolean',
    status = 'status',
    control = 'control'
}
